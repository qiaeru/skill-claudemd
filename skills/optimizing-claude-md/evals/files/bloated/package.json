{
  "name": "users-api",
  "version": "2.4.1",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "build": "node scripts/build.js",
    "test": "vitest run",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "codegen": "openapi-typescript openapi.yaml -o src/generated/types.ts"
  },
  "dependencies": {
    "express": "^5.0.0",
    "pg": "^8.12.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "openapi-typescript": "^7.0.0",
    "vitest": "^2.0.0"
  }
}
